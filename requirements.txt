# Runtime dependencies for the Django site and the twilio_managers daemons.
#
# Pin DIRECT dependencies only and let pip resolve transitives — that way a
# version bump (e.g. cryptography) can't conflict with a stale transitive pin.
# Dev, test, and ops tooling lives in requirements-dev.txt.

# Web framework + API
Django==5.2.14
djangorestframework==3.16.0
django-extensions==4.1
gunicorn==23.0.0

# Database driver (psycopg 3; Django 5.2 uses it when importable)
psycopg[binary]==3.2.9

# Email — prod EMAIL_BACKEND = django_ses.SESBackend
django-ses==4.4.0

# Phone numbers — PhoneNumberField; the backend is NOT installed automatically
django-phonenumber-field==8.1.0
phonenumbers==9.0.5

# Front-end static assets
django-jquery==3.1.0

# AI + SMS integrations
anthropic==0.109.1
twilio==9.6.1

# Content rendering / feeds / geo
markdown2==2.5.3
feedgen==1.0.0
geographiclib==2.0
arrow==1.3.0

# Utilities
requests==2.33.0
python-dateutil==2.9.0.post0
python-dotenv==1.1.0
pytz==2025.2
