server {
    listen 80;
    server_name 44.230.244.94 paulcarroll.site;
    location /static/ {
        root /home/{{ username }}/source;
    }
    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}
